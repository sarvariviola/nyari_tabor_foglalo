<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Nyaritabor" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/pink</item> <!-- ActionBar háttere -->
        <item name="colorPrimaryDark">@color/pink</item> <!-- Status bar -->
        <item name="colorAccent">@color/pink</item>

        <!-- ActionBar szöveg és ikon színe -->
        <item name="actionBarTheme">@style/ActionBarOverlay</item>
    </style>

    <style name="ActionBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>


    <style name="ItemDetailText" parent="TextAppearance.AppCompat.Subhead"/>
    <style name="ItemTitle" parent="TextAppearance.AppCompat.Headline"/>
    <style name="ItemDetailTitle" parent="TextAppearance.AppCompat.Headline"/>
</resources>

